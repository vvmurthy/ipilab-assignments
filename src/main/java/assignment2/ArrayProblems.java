package assignment2;

public class ArrayProblems {


    /**
     * Reverses an array so that the element in
     * index 0 is in index `n.length - 1`, the
     * element in index 1 is in index `n.length - 2`,
     * and so on.
     *
     *
     * @param n 1D built-in array to be reversed
     * @return 1D reversed built-in array
     */
    public static int[] reverseArray(int[] n){
        return new int[1]; // TODO: delete and replace
    }

    /**Finds the max value in a given 2D array
     * n. The max value is the largest value contained
     * in the array.
     *
     * @param n the 2D array to find the maximum value in
     * @return the maximum value in the array `n`
     */
    public static double findMaxValue(double[][] n){
        return 0; // TODO: delete and replace
    }
}
