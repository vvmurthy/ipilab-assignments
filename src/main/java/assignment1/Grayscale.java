package assignment1;

public class Grayscale {

    /**
     * Returns the grayscale value of a pixel,
     * or -1 if the RGB values are invalid.
     *
     * Grayscale value is defined as the mean of the
     * r, g, b components of a pixel, rounded to the
     * nearest integer.
     *
     * @param r red channel pixel value
     * @param g green channel pixel value
     * @param b blue channel pixel value
     * @return -1 if r,g,b is invalid or the grayscale value
     * of the pixel
     */
    public static int convertToGrayscale(int r, int g, int b){
        return 0; // TODO: delete and replace
    }

    /**
     * determines if a pixel is in grayscale
     * A pixel is in grayscale if all channels are
     * equal and the pixel value is between 0 and 255.
     *
     * @param r red channel pixel value
     * @param g green channel pixel value
     * @param b blue channel pixel value
     * @return false if not in gray scale, true otherwise
     */
    public static boolean inGrayScale(int r, int g, int b){
        return false; // TODO: delete and replace
    }

}
